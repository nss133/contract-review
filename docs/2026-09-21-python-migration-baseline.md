# Python 전환 전 기준점 — v1.90.8

2026-09-21. 현재 JavaScript 기반 오프라인 앱의 누적 변경을 커밋하고 기존 origin 추적 브랜치에 push하는 작업이다. Python 전환 구현은 후속 작업으로 진행한다.

## 현재 동작

- 현재 계약 원문에 근거한 약정 존재 자동판정, 등록 표준 연결, 실제 제출 별첨 우선, 수기 판정 보존.
- 원문·확인 완료·검토할 항목의 삼단 화면과 리포트·의견 내보내기.
- 분석 Worker, 참고 검색 지연 실행, 조항 부분 판정과 검색 색인 재사용.
- 종합의견 편집 초안 보존, 저장 실패·아카이브 원문 축약 저장 안내.
- v1.90.8의 네이비·블루 헤더와 오렌지 버튼 표시.

## 이번 재검증

- Python 전체: 107개 통과.
- Node 전체: 1,626개 통과, 실패·취소·생략 0.
- build_html.build를 app_only=True로 임시 경로에 실행: 활성 체크 184개 빌드 스모크 통과. 기존 배포 파일은 재생성하지 않았다.
- 격리 Chrome UI: 47개 통과. v1.90.7 대비 컨트롤·문구·매핑·자동판정 보존, 화면 폭·인쇄 확인.
- 리포트 사전점검: 14개 통과. 의견 초안·저장 실패·재시도·실제 내보내기·계약별 격리 확인.
- 위 브라우저 시험의 앱 예외·외부 요청 0건.
- verify_offline_distribution.py: 소스 87개 모듈, HTML·업데이트·ZIP 일치와 ZIP 무결성 확인.
- git diff --check 통과.

HTML SHA-256: 774f97531ce146fd4a6347655cc6e9ea21f61909aa753a1e2c96513244a52824

ZIP SHA-256: a44f56147d85e86f4ccd304b5eeec366c3ba2f5f16f6565f179d434781496c8d

## 후속 전환의 기준

Python 전환 방식과 배포 구조는 아직 결정하지 않았다. 기존 판정·근거·수기 의견·저장자료와의 호환성을 비교할 수 있도록 현재 소스·시험·지식·배포 업데이트를 기준점으로 보존한다.

실제 폐쇄망 장비 성능·실계약 정확도·법령 최신성 전체를 이번에 검증한 것은 아니다. 기존 제한은 v1.90.8-validation.md와 v1.90.7-validation.md를 따른다. 미저장 초안은 현재 탭 메모리 기준이다.
