# v1.90.8 색감 개선 검증

검증일: 2026-09-19.

최종 HTML SHA-256: 774f97531ce146fd4a6347655cc6e9ea21f61909aa753a1e2c96513244a52824.

## 변경 경계

앱 표시 변경은 src/ui_layout.css에 한정한다. VERSION과 안내·시험 파일도 갱신했다. 리포트 헤더·주요 버튼의 정적인 그라데이션, 밝은 메뉴·본문, 절제된 테두리·그림자를 적용했다. 계약 분석·판정·저장 JavaScript는 이번 변경에서 수정하지 않았다. 인쇄에는 화면 전용 장식을 적용하지 않는다.

## 시험 결과

- Node 1,626개·Python 107개 통과.
- 별도 Chrome 프로필의 UI 점검 47개 통과. v1.90.7 기준본과 초기/분석 후 컨트롤, 전체 체크리스트 문구, 매핑·자동판정 결과 동일. 입력 전체 폭·삼단·메뉴·수기 의견·완료·차트 동선을 보존했다.
- 그라데이션 적용, 중립색 메뉴·판단 제목, 장식의 애니메이션·필터·배경 블러 없음, 판정별 조항 테두리 색 구분, 인쇄 시 그라데이션 제거·짙은 제목을 확인했다. 1440/1100/800px에서 입력·검토·리포트 가로 넘침 없음.
- 의견 초안·커서·계약별 격리·실제 내보내기·저장 실패/재시도·아카이브 실패/축약 저장 등 사전점검 14개 통과.
- UI·사전점검의 앱 예외·외부 요청 0건. 입력·검토·미판정 리포트 화면을 합성 계약으로 캡처하고 육안 확인했다.
- 대용량 성능 회귀 통과: 합성 태깅 500건·근거 26,000행·코퍼스 29건 최초 분석 1.351초·재분석 0.381초·자동판정 13건. 연결 편집 21.7~30ms, 281조항·페이지 CPU4 조건 편집 117.3ms·전체 매핑 재실행 0회. 로컬 단일 측정이며 폐쇄망 장비의 성능 보장은 아니다.

인쇄 추가 시험에서 브라우저 매체 전환 직후 계산 색이 이전 화면색으로 읽히는 간헐 실패를 관찰했다. 동일 앱에서 재실행 시 통과함을 확인하고, 매체 전환 후 두 렌더 프레임을 기다린 뒤 검사하도록 시험 도구를 보완했다. 이 현상을 저장·판정 엔진의 오류로 취급하지 않았다.

Playwright 미설치로 기존 CDP 시험 도구를 사용했다. 실제 사용자의 열린 브라우저나 자료 저장소는 조작하지 않았다. 공개 내장 지식은 기존 배포본과 동일하고 비공개 코퍼스를 포함하지 않는다. 기존 법령 원문 미발견 빌드 경고는 이번 디자인 작업의 보완 범위가 아니다.

모든 기능·파일 형식·접근성 기준의 전수 검증 또는 폐쇄망 실장비 시험은 아니다. 이전 버전의 탭 메모리 초안 보존 한계도 동일하다.
