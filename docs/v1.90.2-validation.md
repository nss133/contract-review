# v1.90.2 로컬 검증

2026-09-18. 폐쇄망 실제 자료·브라우저·장비를 사용한 검증이 아님.

- 전체 Node 회귀: 1,563 통과, 실패·취소·생략 0.
- 전체 Python 회귀: 107 통과.
- 최종 HTML 격리 Chrome: 완결성 경고·UI·미완료 차단 0, 조항 구역 1개 유지, 자동판정 9건 유지. 과거 수기 의견은 새로고침 후 동일 계약 재입력·재분석에도 보존됨. 남은 구형 경고를 강제로 주입해도 완료를 차단하지 않음.
- 통합 체크리스트 표시: 13개 카드, 자동판정 9개, 엔진 질문 불변, 수기 의견 보존. 근거 버튼 최대 하나 유지.
- 자동판정 생명주기: 최초·재분석·재실행·본문 변경 시 기존 자동판정 철회·수기 보존·사용자 중지/재개·표준약정 업로드/재시작·원문 평가자료 저장 후 재분석 통과. 브라우저 오류·외부 요청 0.
- 배포 검사: HTML/업데이트/ZIP 버전 및 내용 정합성 확인. 검증 HTML SHA-256: 126045b99cdabe4dd3f26476286b95726d98bf4525ce089228512393f2238f89.
- 공통 체크리스트 데이터·판정 정책·약정 인식·표준 자동판정·판정 저장 모듈 해시는 변경 전과 동일함.

새 브라우저 시험의 첫 실행은 입력이 자동 저장된다는 시험 작성자의 가정 때문에 재분석할 본문이 없어 실패했음. 앱은 계약 본문을 자동 복원하지 않으므로 시험에서 동일 본문을 다시 열도록 수정하고, 저장된 판단은 재주입하지 않은 채 보존 여부를 재검증하여 통과함.

참고 검색 성능 변경은 미구현임. 합성 계측 결과와 실제 환경의 한계는 [출처 검색 성능 계획](source-search-performance-plan.md)에 기록함. 완결성 제외만으로 폐쇄망의 3분 지연이 해결되었다고 판단하지 않음.
