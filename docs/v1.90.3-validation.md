# v1.90.3 검증 기록

2026-09-18. 최종 HTML SHA-256: 15093c8128101745946077b2ce3fae13a8d2a0653db943a3439c6273a22bec4d.

## 범위와 결과

- 전체 Node 1,576개 통과, 실패·취소·생략 0. Python 107개 통과.
- 기존 전수 참고 검색과 개선 검색은 합성 질의 54개·직접 연결·동점·중복 태그·캐시 변경/변조/상한 경계에서 결과·점수·정렬이 동일함.
- 초기 분석 경로의 태깅 참고 검색 호출을 없애고, 실제 Worker의 별도 참고 요청이 매핑 엔진을 호출하지 않는 것을 확인함.
- 미조회/검색 중/완료/실패/입력 변경 상태, 중복 요청 공유, 검색 취소·재시도·이전 결과 폐기, 출처의 중첩 제자리 변경 감지를 검증함.
- 조항 이동·원위치 복귀·자동매칭 확인·계약 전반 전환은 해당 체크만 판정함. 다른 항목의 자동 근거·수기 의견 보존, 본건 자동저장 이후 부분 갱신 유지, 출처/범위/날짜 변경 시 재분석 분기를 확인함.
- 전체 재분석과 부분 갱신 모두 저장된 의견 대상을 먼저 반영함. 계약 전반으로 옮긴 항목에 이전 자동판정이 남지 않고, 조항 대상으로 복귀하면 현재 원문으로 다시 판정함.
- 브라우저에서 판정 13건·매핑·근거·②열 위치가 v1.90.2와 동일함. 참고 검색 중 작성한 수기 의견과 실제 출처 변경 후 재분석 시 수기 의견도 유지함.
- 자동판정 생명주기: 최초/재분석/새로고침, 본문 변경 시 자동판정 철회, 수기 우선, 중지/재개, 표준약정 업로드·재시작, 내부 평가자료 자동저장 후 재분석 통과. 오류·외부 요청 0.
- 완결성 자동 경고·UI·완료 차단 0, 과거 메모 재시작 보존, 통합 체크리스트 13개·자동 확인 9개 표시 회귀 통과.
- 전용 실행기 시험: 합성 XLSX 태깅 1건·검토이력 2건이 새로고침과 시험용 업데이트 후 유지됨. 실행기 내부 화면의 실제 Worker 분석에서 관할·부가세 자동판정 2건을 확인함.
- 공통 체크리스트·판정 정책·약정 인식·표준 자동판정·판정 저장 모듈은 변경 전 해시와 동일함. 판정 기준을 완화하여 속도를 높인 것이 아님.

## 브라우저 성능

동일 합성 태깅자료 500건·고유 근거 26,000행·코퍼스 29건·본문 2,821자. 실제 앱 분석 버튼을 실행했으며 이 화면에서 활성 체크는 28개임. 각 버전 최초/동일 재실행 각 1회로 통계적 평균이 아님.

| 경로 | v1.90.2 | v1.90.3 |
|---|---:|---:|
| 초기 분석·화면 완료 | 3.970초 | 1.339초 |
| 동일 입력 재분석 | 1.960초 | 0.362초 |
| 자동판정 결과 | 13건 | 동일 13건 |

개선본의 참고 후보는 상세 조회 시 별도 실행하여 약 1.74초 뒤 90개 후보를 표시했음. 이 시간은 기본 검토 완료를 차단하지 않음. 조항 수정 5종은 22~29ms, 전체 매핑 호출 0, 대상 외 체크 재판정 0이었음. 281조항 합성 화면의 페이지 CPU 4배 지연 조건에서는 연결 수정 120ms·전체 매핑 호출 0이었음.

브라우저가 Worker CPU 지연 설정을 지원하지 않아 4배 조건은 페이지에만 적용됨. 실제 폐쇄망 장비·HWP/PDF 원문 추출 시간·실계약 자동판정률·메모리 상한을 측정한 결과가 아님. 전체 화면 재렌더링은 남아 있으며 수천 조항 환경의 속도를 보장하지 않음.

재현: build/test_v1903_performance.mjs. 검색 단독 동등성/측정: build/profile_reference_index.mjs. 관련 상태·취소/입력 변경 회귀는 v190_analysis_runtime, v189_runtime_cache, v1903_partial_update, v1903_reference_index 시험에 포함됨. 사용자 원문·태깅자료·코퍼스는 변경하거나 반출하지 않았음.
