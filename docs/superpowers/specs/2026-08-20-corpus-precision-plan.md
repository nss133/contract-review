# 코퍼스 기반 정밀도 향상 계획 (13차)

- 작성: 2026-08-20 · 승인: 손남수 (계획 수립 세션에서 확정, 집행은 별도 세션)
- 원료 데이터: `~/Downloads/edge-corpus.json` — 폐쇄망 Edge v1.25에서 콘솔 반출한 누적 코퍼스.
  **27계약 · 판정 498건 · 코멘트 106건 · 활동일 2026-08-06~08-19 · 검토자 손남수.**
  (`contract-review-corpus-backup-1.json`은 8/6 시점 구본으로 edge본의 부분집합 — 사용 안 함)
- 리포 상태: 브랜치 `feat/structured-assessment`, v1.30.1(7e48779)까지 12차 P0~P4 완료
  (문장 요건 auto_clear·자동판정·성질결정 서브축·3분할). **폐쇄망 배포본은 아직 v1.25.**

## 집행 결과 (2026-08-20)

- P0 완료: 코퍼스 분석·전후 비교 리포트와 비공개 실계약 임포터 구현. 실계약은
  `data/private/`에만 적재하고 Git 추적에서 제외함.
- P1 완료: `statement / prohibition / definition` 문장 모드, 체크별 `auto_verdict` 안전장치,
  사실기재형 체크의 보수적 자동승격 스펙을 구현함.
- P2 완료: 부속서류 직접 확인은 `covered_by_subdoc`, 계약 참조·검토자 선언은
  `referenced_subdoc`으로 분리해 부재알람과 구별함.
- P3 완료: 전문+판정이 모두 있는 32건(617개 판정)을 현재 엔진으로 재실행하고 증거 게이트를
  교정함. 최초 보고한 **39건→24건**은 `addressed/verify→해당없음`만 센 좁은 지표였으며,
  `consider→해당없음`과 당시 수동 활성 모듈을 구분하지 않아 전체 화면 오탐 지표로는 불충분했다.
  문장 요건은 76건에서 충족됨. 자동판정 안전정책까지 적용한 실제 자동완료 대상은 **53건이며
  기존 사람 판정과 53/53 이상없음으로 일치**함. 원문 없는 재지정 라벨은 추정 교정을 하지 않음.
- P3 잔여 교정(v1.32.0): 평가기를 자동 감지 컨텍스트와 검토자 복원 컨텍스트로 분리하고,
  전체 화면 노출(`addressed/verify/consider`) 오탐·이슈 재현율을 함께 측정함. 같은 평가기 기준:
  - 자동 감지: **오탐 30/334(9.0%) → 8/276(2.9%)**, 이슈 검출 **5/5 → 5/5**,
    자동판정 **53/53 이상없음 유지**.
  - 검토자 복원: **오탐 70/476(14.7%) → 47/421(11.2%)**, 이슈 검출
    **14/15 → 15/15**, 자동판정 **55/55 이상없음 유지**.
  - 종전 “누락 12건”은 아카이브에 수동 모듈 선택값이 없어 발생한 평가 오류였다. 판정 체크로
    모듈을 복원하고 현행 유형 밖 31건·컨텍스트 게이트 17건을 제외하면 실제 기준선 누락은
    ITCL-07 1건이며, 이를 `tier: conditional`로 복원함.
- 회귀 고정: FIN-SEC-03 질권 문서 억제/근저당 문서 유지, 의미 decoy/positive 짝,
  일반 행사위탁/금융규제 업무위탁 범위 짝을 추가함. v1.32.0 전체 회귀·빌드는 P4 절차로 검증.

## 0. 실측 진단 (edge-corpus 분해 결과 — 계획의 근거)

시스템 평가 → 사람 판정 대응쌍 432건:

| 시스템 평가 | n | 사람 판정 분포 | 해석 |
|---|---|---|---|
| evidence_found(확정) | 104 | 이상없음 96(92%) · 해당없음 6 · 의견 2 | 확정 매칭 정밀도 이미 높음 — 자동판정 정책 타당성 입증 |
| possible_evidence(verify) | **235** | 이상없음 186(79%) · 해당없음 41 · 의견 8 | **최대 병목** — 대부분 클릭 노동 |
| evidence_not_found(부재알람) | 58 | 이상없음 31 · 해당없음 22 · 의견 5 | "이상없음 31" 다수는 부속서류 자동기재의 집계 왜곡(아래 §P2) |
| not_surfaced | 35 | 이상없음 33 | PRIV-19/20/21 등 — 부속서류 covers 판정분 |

### 진단 A — verify→이상없음 100% 반복 체크 (자동승격 후보 상위)
CMN-02(16/16) · CMN-11(10/10) · CMN-06(9/9)* · CMN-12(9/9) · CMN-20(8/8) · CMN-21(7/7) ·
ITDL-06(7/8) · CMN-08(6/7) · CMN-09(6/6)* · PRIV-14(5/5) · SP-DEL-01(5/5) · CMN-15(4/4) ·
CMN-16(4/4)* · SP-DEL-06(4/4) · PRIV-04(4/4) · CMN-04(3/3)* · SP-DEL-05(4/5) · SP-DEL-07(4/5) ·
SP-DEL-08(4/5) · SP-DEL-11(4/5) · PRIV-13(3/4) · CMN-14(3/4) · CMN-03(3/4)*
(*표시는 12차에서 auto_clear 기부여 — v1.28 이후 해소 예상, 나머지가 이번 대상)

### 진단 B — 금지형 공백 (엔진 확장 필요)
CMN-20(양도 제한)의 정상 조항은 "양도할 수 없다"로 끝남. 현행 `src/sentence.js`의
`isNegated`가 부정 종결을 일괄 제외하므로 **금지형 체크는 구조적으로 자동화 불가** → §P1-①.

### 진단 C — verify→해당없음 오탐 (관련성 오탐)
SP-DEL-08-2(4) · ALL-PII-03(4) · CMN-18(3) · SP-DEL-04(3) · PRIV-10(2) · CORE-02(2)

### 진단 D — 오부착(재지정) 상위
FIN-SEC-03(재지정 2/관측 2 — **항상 오부착**) · CMN-21(2/6) · CMN-09(2/4) · SP-DEL-11(2/3) ·
CMN-15·SP-DEL-03·PRIV-04·CMN-05-2·SP-DEL-08-2·ITDL-01(각 1). 재지정 기록은 사람이 지목한
정답 조항 = 최고급 신호이나 **코퍼스에는 조항 인덱스만 있고 원문 없음** → 아카이브 필요(§P0-②).

### 진단 E — 부속서류 집계 왜곡
PRIV-03/05/06 등 "부재알람→이상없음" 코멘트 7회가 전부 "표준 개인(신용)정보
보안관리약정서(2025.01) 체결로 반영 — 별첨 체결·간인 확인" = subdoc 자동기재의 정상 동작이
system_assessment `evidence_not_found`와 짝지어져 리콜 실패처럼 집계됨. 진짜 리콜 실패
후보는 SP-DEL-02(2)·PRIV-15(2) 등 소수. SP-DEL-05 코멘트("시정조치 요구·해지권 보유")는
위임형 구제 체계 — 12차 P3(service_scope·SP-DEL-12)가 이미 해소.

### 진단 F — LLM 대응쌍 0건
폐쇄망에서 로컬 AI 미사용. LLM 축 평가는 이 데이터셋으로 불가(후속 반출본 과제).

## P0 — 측정 인프라 (소, 1세션)

① `build/corpus_tuning_report.py` 신설: 코퍼스 백업 JSON 경로를 인자로 받아
   - 시스템평가→사람판정 대응표, verify→이상없음 반복률 순위(자동승격 후보),
     verify→해당없음 순위(오탐), 부재알람 분해(반영/해당없음), 재지정률 순위,
     origin 분포, (있으면) LLM 대응쌍을 출력.
   - 두 코퍼스 파일을 받아 **전후 비교 모드**(개선 사이클 측정용) 지원.
   - 테스트: tests/test_corpus_tuning_report.py (고정 픽스처).
② **Edge 아카이브 반출 임포터**: 사용자가 폐쇄망 Edge 콘솔에서 뽑은 아카이브 레지스트리
   (계약 전문)와 별도 `cr-verdict-*` 덤프(계약별 판정)를 결합해 비공개 튜닝 케이스로 변환하는
   `build/import_archive_cases.py`. 재지정 덤프가 있으면 조항 라벨도 선택적으로 결합함.
   ⚠️ 레지스트리는 4MB 초과 시 전문이 지문으로 강등된 항목이 있을 수 있음 — 전문 있는 것만 변환.
   ⚠️ 반출 파일은 실계약이라 **리포에 커밋 금지** (tests/goldset/cases에 넣더라도 사내 반출
   규정 확인 전에는 로컬 전용 — .gitignore 처리 또는 별도 폴더).
   **집행 결과**: 아카이브 35건 중 32건을 전문+판정 결합 케이스로 변환함.

## P1 — verify 대량 자동화 (중, 2세션, 최대 지렛대)

① `src/sentence.js`에 **금지형 모드** 추가: `auto_clear.expect: "prohibition"` —
   부정·금지 종결("~할 수 없다"·"~하여서는 아니 된다"·"금지한다")이어야 충족으로 인정
   (기본값 "statement"는 현행 긍정 종결). isNegated를 expect에 따라 반전 적용하되
   단서·정의·공란 제외는 공통 유지. matcher·validate.py·schema.md 동기화.
② 진단 A의 미커버 체크 ~16개에 auto_clear 스펙 부여(사실기재·존재확인형 한정, 판단형 제외):
   - CMN-02(목적: "…을 목적으로 한다", require 없음 — 단 weak-role 목적 조항 관통이므로
     정의·인용 오탐 케이스 짝 고정 필수)
   - CMN-11(손배: [손해, 배상]+[배상한다/책임을 진다]) · CMN-12(통상손해 특약 존재)
   - CMN-20(양도: expect prohibition, [양도]+금지 종결) · CMN-21(완전합의)
   - CMN-08(해지·해제 사유 — 존재확인까지만, 균형성 판단은 사람 몫이므로 심각도·문언 보수적으로)
   - CMN-14 · CMN-15(비밀정보 정의: 정의 문장이 정상인 체크 — isDefinition 제외 규칙과 충돌,
     `allow_definition` 플래그 검토) · CMN-18
   - PRIV-04(위탁 목적·범위) · PRIV-13(교육·감독) · PRIV-14(목적외 금지: expect prohibition)
   - SP-DEL-01(검수) · SP-DEL-05/06/07/08(하자담보 4종) · SP-DEL-11 · ITDL-06(SLA)
   각 스펙은 코퍼스 수치를 주석으로 병기(예: "verify→이상없음 16/16, 2026-08-19 코퍼스").
③ 기대 효과: 이 데이터셋 기준 verify 235건 중 대상 체크 판정 ~120건 — 문장 요건 통과분만
   자동이므로 보수적으로 절반이면 계약 27건당 클릭 ~60회 절감. 실측은 P4 재반출로 검증.
④ 검증: 체크별 goldset/단위 케이스 짝(충족→자동, 부정·공란→수동), node 전체 회귀,
   vm 도메인 스모크(scratchpad/domsmoke.js 패턴 — 계획 세션 스크래치패드 소멸 시 재작성).

## P2 — 부속서류 집계 정합 (소, 1세션)

- `src/assessment.js` `_classification`에 부속서류 커버 상태 분리: coverage가 consider라도
  subDocCov/refCov에 잡힌 항목은 `system_assessment: "covered_by_subdoc"`(route human_confirm)
  로 기록 — 앱에서 state.subDocCov를 assessment 컨텍스트로 전달.
- `src/loop.js` 집계·`reviewRoute`가 새 상태를 인지(evidence_not_found와 분리 집계).
- 효과: 부재알람 통계에서 별첨 커버 정상분이 빠져 **진짜 리콜 실패가 보이게 됨**.
- 하위호환: 기존 코퍼스의 evidence_not_found 집계는 그대로 읽힘(신규 키만 추가).

## P3 — 오탐·오부착 교정 (중, 2세션, P0-② 수령 의존)

- 진단 D: 아카이브 원문에서 재지정 정답 조항(human_clause_index)의 표제·어휘를 추출해
  FIN-SEC-03(근저당 채권최고액 — 2/2 오부착) 우선 교정, CMN-21·CMN-09·SP-DEL-11 후속.
- 진단 C: SP-DEL-08-2(보증보험 어휘 스침)·ALL-PII-03·CMN-18·SP-DEL-04 오탐을
  아카이브 케이스로 고정한 뒤 게이트(키워드 정밀화·requires·precondition) 조정.
- 원칙: **게이트는 항상 짝으로 케이스 고정**(억제 케이스 + 억제되면 안 되는 케이스).
  원문 없이 감으로 키워드를 고치지 말 것 — P0-② 미수령 시 이 단계는 보류.

## P4 — 배포·사이클 (소 + 운영)

1. P1·P2까지 묶어 VERSION 1.31.0 재빌드(빌드가 배포 zip 자동 생성) → 폐쇄망 배포.
   **전제 인식: 폐쇄망은 v1.25라 12차 개선(자동판정·성질결정)도 아직 미적용 — 이번 배포가
   12차+13차를 한꺼번에 실사용에 올림.** 안내서 docx 본문 동기화(3분할·자동판정 설명) 포함.
2. 2~3주 사용 후 코퍼스 재반출(v1.30.1+는 백업 버튼에 날짜·건수 파일명과 성공 메시지 있음)
   → `corpus_tuning_report.py` 전후 비교로 효과 실측(verify 정탐률·자동판정 커버리지·알람 정탐률).
3. 이 반출→튜닝→배포 루프를 상시 사이클로.

### v1.32.0 잔여 8건의 해석

- 적용범위·정책 판단 5건: CH-02 2건(위촉/고문을 판매채널로 볼지), ALL-PII-02 1건
  (법정 고지사항을 본계약에 둘지 동의서에 둘지), SP-DEL-02 1건(렌탈 지체상금 필요성),
  SP-DEL-05 1건(사무관리 용역의 도급/위임 성질).
- 사람 라벨과 문언 충돌 3건: CMN-03(대가 조항 존재), CMN-05-2(실제 지연배상 조항),
  CMN-16(종료 후 비밀유지 존속 문언 존재). 이 3건은 매칭 오류로 억제하면 정탐을 잃으므로 유지.
- 따라서 추가 감소는 키워드 미세조정보다 계약 성질·업무 적용범위에 대한 사용자 입력 또는
  라벨 재확인이 선행되어야 한다. 실계약 32건에 맞춘 무근거 억제는 하지 않는다.

## 결정·확인 대기 사항

- [x] Edge 아카이브 덤프 수령(2026-08-20): `~/Downloads/cr-archive-registry.txt` —
      **35건 전부 계약 전문 포함**(지문 강등 0), 8/3~8/20, 코퍼스 27건 100% 대응 + 추가 8건.
      유형: procurement 24 · alliance 3 · outsourcing 3 · channel 2 · investment 2 · 미확정 1.
- [x] 계약별 판정 덤프 수령(2026-08-20, 보강): `~/Downloads/startswith.txt` —
      cr-verdict-* 61계약분 · 판정 1,027건 · 2026-07-10~08-20.
      **아카이브 35건 중 32건이 전문+계약별 판정 완비 = 실계약 골드셋 1순위 후보.**
      ⚠️ 두 파일 모두 실계약 원문·판정 — 리포 커밋 금지, 집행 시 로컬 전용 폴더로 이동.
- [x] 실계약 아카이브의 로컬 전용 보관 폴더 확정: `data/private/` + `.gitignore`.
- [x] CMN-08처럼 "존재+균형성" 복합 체크는 증거만 확정하고 `auto_verdict: false`로 유지.
- [ ] main 병합·원격 푸시(12차 커밋 6개 포함) 시점.

## 집행 메모

- 브랜치: feat/structured-assessment 이어서 진행(또는 main 병합 후). 버전 규칙:
  피드백 라운드 반영 시 minor 상향 + 재빌드(루트 VERSION이 단일 소스).
- 테스트 기준선(2026-08-20): node 324 · pytest 77 전부 통과.
- 관련 코드: 문장 요건 src/sentence.js, 승격 게이트 src/matcher.js(autoClearEval·decideTier),
  자동판정 src/app.js(applyAutoVerdicts), 집계 src/loop.js, 평가 src/assessment.js,
  검증 build/validate.py(auto_clear·service_scope), 스키마 knowledge/schema.md.
